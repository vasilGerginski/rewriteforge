[project]
name = "llm-adapters"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.30.0",
    "openai>=1.30.0",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio"]